# application.yaml은 중요한 정보가 기재되어있어서, 깃허브에 파일을 업로드하지 않습니다.

spring:
  application:
    name: springCommunity

# h2의 경우 인텔리제이의 h2와 스프링부트에서 동작하는 db는 각각 분리된 개별 db이다.
# 만약 스프링부트에서 실행하고싶은 h2가 있다면 스프링부트에서 자동완성하는 h2를 사용하면 안된다.

  datasource:
#    url: jdbc:h2:mem:community;MODE=MySQL
#    postgresql을 사용할 것이고 ://주소:port번호/db이름
    url: jdbc:postgresql://localhost:5432/community
    driver-class-name: org.postgresql.Driver
    username: postgres #ql 지우기
    password: 1234
  sql:
    init:
      #mode: always
      mode: never

  mail:     # 메일을 보내기위한 계정 설정 추가 방법
    host: smtp.gmail.com
    port: 587
    username: naroom0129@gmail.com
    password: fbsx axnv hhgc wqeb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 5MB # 1개 파일 업로드 가능한 최대 크기
      max-request-size: 5MB # 모든 파일의 크기가 총 5MB까지만 가능
      # 프론트엔드에서 백엔드로 데이터를 넘기고, 백엔드에서 프론트엔드로 전달받은 데이터를 저장할 때
      # 한 파일당 회사에서 업로드 가능하도록 지정한 크기까지만 허용 제한
      # 전체 파일 또한 업로드 가능하도록 지정한 크기까지만 허용 제한


mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

# 쿠키세션 30분 설정 추가
server:
  servlet:
    session:
      timeout: 30m

# 나의 프로젝트에서 클라이언트가 업로드한 프로필 사진을 저장하는 폴더경로
file:
  upload-dir: uploads/profile
